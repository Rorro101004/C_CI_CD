name: C CI Calculator (Manual)

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    # 1. Descargar tu código
    - name: Checkout code
      uses: actions/checkout@v4

    # 2. Compilar y Ejecutar (TODO EN UNO)
    # Usamos la barra vertical '|' para poner varios comandos seguidos
    - name: Compile and Run Tests manually
      run: |
        gcc -DTEST starter.c test_challengue02.c -o test_runner
        ./test_runner
